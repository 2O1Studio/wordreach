import { React } from "react";
import { Button } from "../../components/Button/Button";

const Home = () => {
  return (
    <div>
      <Button center to="/challenge">
        Play now
      </Button>
    </div>
  );
};

export default Home;
